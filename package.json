{
  "name": "manga-viewer",
  "version": "1.0.0",
  "description": "A responsive manga viewer with touch gestures and zoom support",
  "main": "index.html",
  "scripts": {
    "lint": "eslint src/ scripts/",
    "build": "npm run lint && tailwindcss -i ./src/input.css -o ./dist/output.css --minify && node scripts/build.js",
    "dev": "tailwindcss -i ./src/input.css -o ./dist/output.css --watch",
    "build:prod": "npm run lint && NODE_ENV=production tailwindcss -i ./src/input.css -o ./dist/output.css --minify && node scripts/build.js"
  },
  "keywords": [
    "manga",
    "viewer",
    "comic",
    "reader"
  ],
  "author": "",
  "license": "Apache-2.0",
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "eslint": "^10.1.0",
    "globals": "^17.4.0",
    "tailwindcss": "^3.4.1"
  }
}
